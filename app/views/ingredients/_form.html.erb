<%= form_for(@ingredient) do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :amount %>
<%= f.text_field :amount %>

<%= f.label :unit %>
<%= f.text_field :unit %>

<br>
<%= f.submit %>

<% end %>